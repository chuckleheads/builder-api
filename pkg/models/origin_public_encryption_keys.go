// Code generated by SQLBoiler (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/pkg/errors"
	"github.com/volatiletech/null"
	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/queries/qm"
	"github.com/volatiletech/sqlboiler/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/strmangle"
)

// OriginPublicEncryptionKey is an object representing the database table.
type OriginPublicEncryptionKey struct {
	ID        int64       `boil:"id" json:"id" toml:"id" yaml:"id"`
	OwnerID   int64       `boil:"owner_id" json:"owner_id" toml:"owner_id" yaml:"owner_id"`
	Name      null.String `boil:"name" json:"name,omitempty" toml:"name" yaml:"name,omitempty"`
	Revision  null.String `boil:"revision" json:"revision,omitempty" toml:"revision" yaml:"revision,omitempty"`
	FullName  null.String `boil:"full_name" json:"full_name,omitempty" toml:"full_name" yaml:"full_name,omitempty"`
	Body      null.Bytes  `boil:"body" json:"body,omitempty" toml:"body" yaml:"body,omitempty"`
	CreatedAt null.Time   `boil:"created_at" json:"created_at,omitempty" toml:"created_at" yaml:"created_at,omitempty"`
	UpdatedAt null.Time   `boil:"updated_at" json:"updated_at,omitempty" toml:"updated_at" yaml:"updated_at,omitempty"`
	Origin    null.String `boil:"origin" json:"origin,omitempty" toml:"origin" yaml:"origin,omitempty"`

	R *originPublicEncryptionKeyR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L originPublicEncryptionKeyL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var OriginPublicEncryptionKeyColumns = struct {
	ID        string
	OwnerID   string
	Name      string
	Revision  string
	FullName  string
	Body      string
	CreatedAt string
	UpdatedAt string
	Origin    string
}{
	ID:        "id",
	OwnerID:   "owner_id",
	Name:      "name",
	Revision:  "revision",
	FullName:  "full_name",
	Body:      "body",
	CreatedAt: "created_at",
	UpdatedAt: "updated_at",
	Origin:    "origin",
}

// Generated where

var OriginPublicEncryptionKeyWhere = struct {
	ID        whereHelperint64
	OwnerID   whereHelperint64
	Name      whereHelpernull_String
	Revision  whereHelpernull_String
	FullName  whereHelpernull_String
	Body      whereHelpernull_Bytes
	CreatedAt whereHelpernull_Time
	UpdatedAt whereHelpernull_Time
	Origin    whereHelpernull_String
}{
	ID:        whereHelperint64{field: `id`},
	OwnerID:   whereHelperint64{field: `owner_id`},
	Name:      whereHelpernull_String{field: `name`},
	Revision:  whereHelpernull_String{field: `revision`},
	FullName:  whereHelpernull_String{field: `full_name`},
	Body:      whereHelpernull_Bytes{field: `body`},
	CreatedAt: whereHelpernull_Time{field: `created_at`},
	UpdatedAt: whereHelpernull_Time{field: `updated_at`},
	Origin:    whereHelpernull_String{field: `origin`},
}

// OriginPublicEncryptionKeyRels is where relationship names are stored.
var OriginPublicEncryptionKeyRels = struct {
	OriginName string
}{
	OriginName: "OriginName",
}

// originPublicEncryptionKeyR is where relationships are stored.
type originPublicEncryptionKeyR struct {
	OriginName *Origin
}

// NewStruct creates a new relationship struct
func (*originPublicEncryptionKeyR) NewStruct() *originPublicEncryptionKeyR {
	return &originPublicEncryptionKeyR{}
}

// originPublicEncryptionKeyL is where Load methods for each relationship are stored.
type originPublicEncryptionKeyL struct{}

var (
	originPublicEncryptionKeyColumns               = []string{"id", "owner_id", "name", "revision", "full_name", "body", "created_at", "updated_at", "origin"}
	originPublicEncryptionKeyColumnsWithoutDefault = []string{"owner_id", "name", "revision", "full_name", "body", "origin"}
	originPublicEncryptionKeyColumnsWithDefault    = []string{"id", "created_at", "updated_at"}
	originPublicEncryptionKeyPrimaryKeyColumns     = []string{"id"}
)

type (
	// OriginPublicEncryptionKeySlice is an alias for a slice of pointers to OriginPublicEncryptionKey.
	// This should generally be used opposed to []OriginPublicEncryptionKey.
	OriginPublicEncryptionKeySlice []*OriginPublicEncryptionKey
	// OriginPublicEncryptionKeyHook is the signature for custom OriginPublicEncryptionKey hook methods
	OriginPublicEncryptionKeyHook func(context.Context, boil.ContextExecutor, *OriginPublicEncryptionKey) error

	originPublicEncryptionKeyQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	originPublicEncryptionKeyType                 = reflect.TypeOf(&OriginPublicEncryptionKey{})
	originPublicEncryptionKeyMapping              = queries.MakeStructMapping(originPublicEncryptionKeyType)
	originPublicEncryptionKeyPrimaryKeyMapping, _ = queries.BindMapping(originPublicEncryptionKeyType, originPublicEncryptionKeyMapping, originPublicEncryptionKeyPrimaryKeyColumns)
	originPublicEncryptionKeyInsertCacheMut       sync.RWMutex
	originPublicEncryptionKeyInsertCache          = make(map[string]insertCache)
	originPublicEncryptionKeyUpdateCacheMut       sync.RWMutex
	originPublicEncryptionKeyUpdateCache          = make(map[string]updateCache)
	originPublicEncryptionKeyUpsertCacheMut       sync.RWMutex
	originPublicEncryptionKeyUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var originPublicEncryptionKeyBeforeInsertHooks []OriginPublicEncryptionKeyHook
var originPublicEncryptionKeyBeforeUpdateHooks []OriginPublicEncryptionKeyHook
var originPublicEncryptionKeyBeforeDeleteHooks []OriginPublicEncryptionKeyHook
var originPublicEncryptionKeyBeforeUpsertHooks []OriginPublicEncryptionKeyHook

var originPublicEncryptionKeyAfterInsertHooks []OriginPublicEncryptionKeyHook
var originPublicEncryptionKeyAfterSelectHooks []OriginPublicEncryptionKeyHook
var originPublicEncryptionKeyAfterUpdateHooks []OriginPublicEncryptionKeyHook
var originPublicEncryptionKeyAfterDeleteHooks []OriginPublicEncryptionKeyHook
var originPublicEncryptionKeyAfterUpsertHooks []OriginPublicEncryptionKeyHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *OriginPublicEncryptionKey) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range originPublicEncryptionKeyBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *OriginPublicEncryptionKey) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range originPublicEncryptionKeyBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *OriginPublicEncryptionKey) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range originPublicEncryptionKeyBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *OriginPublicEncryptionKey) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range originPublicEncryptionKeyBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *OriginPublicEncryptionKey) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range originPublicEncryptionKeyAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *OriginPublicEncryptionKey) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range originPublicEncryptionKeyAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *OriginPublicEncryptionKey) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range originPublicEncryptionKeyAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *OriginPublicEncryptionKey) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range originPublicEncryptionKeyAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *OriginPublicEncryptionKey) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range originPublicEncryptionKeyAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddOriginPublicEncryptionKeyHook registers your hook function for all future operations.
func AddOriginPublicEncryptionKeyHook(hookPoint boil.HookPoint, originPublicEncryptionKeyHook OriginPublicEncryptionKeyHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		originPublicEncryptionKeyBeforeInsertHooks = append(originPublicEncryptionKeyBeforeInsertHooks, originPublicEncryptionKeyHook)
	case boil.BeforeUpdateHook:
		originPublicEncryptionKeyBeforeUpdateHooks = append(originPublicEncryptionKeyBeforeUpdateHooks, originPublicEncryptionKeyHook)
	case boil.BeforeDeleteHook:
		originPublicEncryptionKeyBeforeDeleteHooks = append(originPublicEncryptionKeyBeforeDeleteHooks, originPublicEncryptionKeyHook)
	case boil.BeforeUpsertHook:
		originPublicEncryptionKeyBeforeUpsertHooks = append(originPublicEncryptionKeyBeforeUpsertHooks, originPublicEncryptionKeyHook)
	case boil.AfterInsertHook:
		originPublicEncryptionKeyAfterInsertHooks = append(originPublicEncryptionKeyAfterInsertHooks, originPublicEncryptionKeyHook)
	case boil.AfterSelectHook:
		originPublicEncryptionKeyAfterSelectHooks = append(originPublicEncryptionKeyAfterSelectHooks, originPublicEncryptionKeyHook)
	case boil.AfterUpdateHook:
		originPublicEncryptionKeyAfterUpdateHooks = append(originPublicEncryptionKeyAfterUpdateHooks, originPublicEncryptionKeyHook)
	case boil.AfterDeleteHook:
		originPublicEncryptionKeyAfterDeleteHooks = append(originPublicEncryptionKeyAfterDeleteHooks, originPublicEncryptionKeyHook)
	case boil.AfterUpsertHook:
		originPublicEncryptionKeyAfterUpsertHooks = append(originPublicEncryptionKeyAfterUpsertHooks, originPublicEncryptionKeyHook)
	}
}

// One returns a single originPublicEncryptionKey record from the query.
func (q originPublicEncryptionKeyQuery) One(ctx context.Context, exec boil.ContextExecutor) (*OriginPublicEncryptionKey, error) {
	o := &OriginPublicEncryptionKey{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for origin_public_encryption_keys")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all OriginPublicEncryptionKey records from the query.
func (q originPublicEncryptionKeyQuery) All(ctx context.Context, exec boil.ContextExecutor) (OriginPublicEncryptionKeySlice, error) {
	var o []*OriginPublicEncryptionKey

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to OriginPublicEncryptionKey slice")
	}

	if len(originPublicEncryptionKeyAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all OriginPublicEncryptionKey records in the query.
func (q originPublicEncryptionKeyQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count origin_public_encryption_keys rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q originPublicEncryptionKeyQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if origin_public_encryption_keys exists")
	}

	return count > 0, nil
}

// OriginName pointed to by the foreign key.
func (o *OriginPublicEncryptionKey) OriginName(mods ...qm.QueryMod) originQuery {
	queryMods := []qm.QueryMod{
		qm.Where("name=?", o.Origin),
	}

	queryMods = append(queryMods, mods...)

	query := Origins(queryMods...)
	queries.SetFrom(query.Query, "\"origins\"")

	return query
}

// LoadOriginName allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (originPublicEncryptionKeyL) LoadOriginName(ctx context.Context, e boil.ContextExecutor, singular bool, maybeOriginPublicEncryptionKey interface{}, mods queries.Applicator) error {
	var slice []*OriginPublicEncryptionKey
	var object *OriginPublicEncryptionKey

	if singular {
		object = maybeOriginPublicEncryptionKey.(*OriginPublicEncryptionKey)
	} else {
		slice = *maybeOriginPublicEncryptionKey.(*[]*OriginPublicEncryptionKey)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &originPublicEncryptionKeyR{}
		}
		if !queries.IsNil(object.Origin) {
			args = append(args, object.Origin)
		}

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &originPublicEncryptionKeyR{}
			}

			for _, a := range args {
				if queries.Equal(a, obj.Origin) {
					continue Outer
				}
			}

			if !queries.IsNil(obj.Origin) {
				args = append(args, obj.Origin)
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(qm.From(`origins`), qm.WhereIn(`name in ?`, args...))
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Origin")
	}

	var resultSlice []*Origin
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Origin")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for origins")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for origins")
	}

	if len(originPublicEncryptionKeyAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.OriginName = foreign
		if foreign.R == nil {
			foreign.R = &originR{}
		}
		foreign.R.OriginOPUEK = append(foreign.R.OriginOPUEK, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.Origin, foreign.Name) {
				local.R.OriginName = foreign
				if foreign.R == nil {
					foreign.R = &originR{}
				}
				foreign.R.OriginOPUEK = append(foreign.R.OriginOPUEK, local)
				break
			}
		}
	}

	return nil
}

// SetOriginName of the originPublicEncryptionKey to the related item.
// Sets o.R.OriginName to related.
// Adds o to related.R.OriginOPUEK.
func (o *OriginPublicEncryptionKey) SetOriginName(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Origin) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"origin_public_encryption_keys\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"origin"}),
		strmangle.WhereClause("\"", "\"", 2, originPublicEncryptionKeyPrimaryKeyColumns),
	)
	values := []interface{}{related.Name, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.Origin, related.Name)
	if o.R == nil {
		o.R = &originPublicEncryptionKeyR{
			OriginName: related,
		}
	} else {
		o.R.OriginName = related
	}

	if related.R == nil {
		related.R = &originR{
			OriginOPUEK: OriginPublicEncryptionKeySlice{o},
		}
	} else {
		related.R.OriginOPUEK = append(related.R.OriginOPUEK, o)
	}

	return nil
}

// RemoveOriginName relationship.
// Sets o.R.OriginName to nil.
// Removes o from all passed in related items' relationships struct (Optional).
func (o *OriginPublicEncryptionKey) RemoveOriginName(ctx context.Context, exec boil.ContextExecutor, related *Origin) error {
	var err error

	queries.SetScanner(&o.Origin, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("origin")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.R.OriginName = nil
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.OriginOPUEK {
		if queries.Equal(o.Origin, ri.Origin) {
			continue
		}

		ln := len(related.R.OriginOPUEK)
		if ln > 1 && i < ln-1 {
			related.R.OriginOPUEK[i] = related.R.OriginOPUEK[ln-1]
		}
		related.R.OriginOPUEK = related.R.OriginOPUEK[:ln-1]
		break
	}
	return nil
}

// OriginPublicEncryptionKeys retrieves all the records using an executor.
func OriginPublicEncryptionKeys(mods ...qm.QueryMod) originPublicEncryptionKeyQuery {
	mods = append(mods, qm.From("\"origin_public_encryption_keys\""))
	return originPublicEncryptionKeyQuery{NewQuery(mods...)}
}

// FindOriginPublicEncryptionKey retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindOriginPublicEncryptionKey(ctx context.Context, exec boil.ContextExecutor, iD int64, selectCols ...string) (*OriginPublicEncryptionKey, error) {
	originPublicEncryptionKeyObj := &OriginPublicEncryptionKey{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"origin_public_encryption_keys\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, originPublicEncryptionKeyObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from origin_public_encryption_keys")
	}

	return originPublicEncryptionKeyObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *OriginPublicEncryptionKey) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no origin_public_encryption_keys provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if queries.MustTime(o.CreatedAt).IsZero() {
			queries.SetScanner(&o.CreatedAt, currTime)
		}
		if queries.MustTime(o.UpdatedAt).IsZero() {
			queries.SetScanner(&o.UpdatedAt, currTime)
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(originPublicEncryptionKeyColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	originPublicEncryptionKeyInsertCacheMut.RLock()
	cache, cached := originPublicEncryptionKeyInsertCache[key]
	originPublicEncryptionKeyInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			originPublicEncryptionKeyColumns,
			originPublicEncryptionKeyColumnsWithDefault,
			originPublicEncryptionKeyColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(originPublicEncryptionKeyType, originPublicEncryptionKeyMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(originPublicEncryptionKeyType, originPublicEncryptionKeyMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"origin_public_encryption_keys\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"origin_public_encryption_keys\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into origin_public_encryption_keys")
	}

	if !cached {
		originPublicEncryptionKeyInsertCacheMut.Lock()
		originPublicEncryptionKeyInsertCache[key] = cache
		originPublicEncryptionKeyInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the OriginPublicEncryptionKey.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *OriginPublicEncryptionKey) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		queries.SetScanner(&o.UpdatedAt, currTime)
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	originPublicEncryptionKeyUpdateCacheMut.RLock()
	cache, cached := originPublicEncryptionKeyUpdateCache[key]
	originPublicEncryptionKeyUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			originPublicEncryptionKeyColumns,
			originPublicEncryptionKeyPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update origin_public_encryption_keys, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"origin_public_encryption_keys\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, originPublicEncryptionKeyPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(originPublicEncryptionKeyType, originPublicEncryptionKeyMapping, append(wl, originPublicEncryptionKeyPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update origin_public_encryption_keys row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for origin_public_encryption_keys")
	}

	if !cached {
		originPublicEncryptionKeyUpdateCacheMut.Lock()
		originPublicEncryptionKeyUpdateCache[key] = cache
		originPublicEncryptionKeyUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q originPublicEncryptionKeyQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for origin_public_encryption_keys")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for origin_public_encryption_keys")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o OriginPublicEncryptionKeySlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), originPublicEncryptionKeyPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"origin_public_encryption_keys\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, originPublicEncryptionKeyPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in originPublicEncryptionKey slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all originPublicEncryptionKey")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *OriginPublicEncryptionKey) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no origin_public_encryption_keys provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if queries.MustTime(o.CreatedAt).IsZero() {
			queries.SetScanner(&o.CreatedAt, currTime)
		}
		queries.SetScanner(&o.UpdatedAt, currTime)
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(originPublicEncryptionKeyColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	originPublicEncryptionKeyUpsertCacheMut.RLock()
	cache, cached := originPublicEncryptionKeyUpsertCache[key]
	originPublicEncryptionKeyUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			originPublicEncryptionKeyColumns,
			originPublicEncryptionKeyColumnsWithDefault,
			originPublicEncryptionKeyColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			originPublicEncryptionKeyColumns,
			originPublicEncryptionKeyPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert origin_public_encryption_keys, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(originPublicEncryptionKeyPrimaryKeyColumns))
			copy(conflict, originPublicEncryptionKeyPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"origin_public_encryption_keys\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(originPublicEncryptionKeyType, originPublicEncryptionKeyMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(originPublicEncryptionKeyType, originPublicEncryptionKeyMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert origin_public_encryption_keys")
	}

	if !cached {
		originPublicEncryptionKeyUpsertCacheMut.Lock()
		originPublicEncryptionKeyUpsertCache[key] = cache
		originPublicEncryptionKeyUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single OriginPublicEncryptionKey record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *OriginPublicEncryptionKey) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no OriginPublicEncryptionKey provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), originPublicEncryptionKeyPrimaryKeyMapping)
	sql := "DELETE FROM \"origin_public_encryption_keys\" WHERE \"id\"=$1"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from origin_public_encryption_keys")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for origin_public_encryption_keys")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q originPublicEncryptionKeyQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no originPublicEncryptionKeyQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from origin_public_encryption_keys")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for origin_public_encryption_keys")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o OriginPublicEncryptionKeySlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no OriginPublicEncryptionKey slice provided for delete all")
	}

	if len(o) == 0 {
		return 0, nil
	}

	if len(originPublicEncryptionKeyBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), originPublicEncryptionKeyPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"origin_public_encryption_keys\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, originPublicEncryptionKeyPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from originPublicEncryptionKey slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for origin_public_encryption_keys")
	}

	if len(originPublicEncryptionKeyAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *OriginPublicEncryptionKey) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindOriginPublicEncryptionKey(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *OriginPublicEncryptionKeySlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := OriginPublicEncryptionKeySlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), originPublicEncryptionKeyPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"origin_public_encryption_keys\".* FROM \"origin_public_encryption_keys\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, originPublicEncryptionKeyPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in OriginPublicEncryptionKeySlice")
	}

	*o = slice

	return nil
}

// OriginPublicEncryptionKeyExists checks if the OriginPublicEncryptionKey row exists.
func OriginPublicEncryptionKeyExists(ctx context.Context, exec boil.ContextExecutor, iD int64) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"origin_public_encryption_keys\" where \"id\"=$1 limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, iD)
	}

	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if origin_public_encryption_keys exists")
	}

	return exists, nil
}
