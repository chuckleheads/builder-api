// Code generated by SQLBoiler (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/pkg/errors"
	"github.com/volatiletech/null"
	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/queries/qm"
	"github.com/volatiletech/sqlboiler/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/strmangle"
	"github.com/volatiletech/sqlboiler/types"
)

// AuditPackageGroup is an object representing the database table.
type AuditPackageGroup struct {
	ID            int              `boil:"id" json:"id" toml:"id" yaml:"id"`
	PackageIds    types.Int64Array `boil:"package_ids" json:"package_ids,omitempty" toml:"package_ids" yaml:"package_ids,omitempty"`
	Operation     null.String      `boil:"operation" json:"operation,omitempty" toml:"operation" yaml:"operation,omitempty"`
	Trigger       null.String      `boil:"trigger" json:"trigger,omitempty" toml:"trigger" yaml:"trigger,omitempty"`
	RequesterID   null.Int64       `boil:"requester_id" json:"requester_id,omitempty" toml:"requester_id" yaml:"requester_id,omitempty"`
	RequesterName null.String      `boil:"requester_name" json:"requester_name,omitempty" toml:"requester_name" yaml:"requester_name,omitempty"`
	GroupID       null.Int64       `boil:"group_id" json:"group_id,omitempty" toml:"group_id" yaml:"group_id,omitempty"`
	CreatedAt     null.Time        `boil:"created_at" json:"created_at,omitempty" toml:"created_at" yaml:"created_at,omitempty"`
	OriginName    null.String      `boil:"origin_name" json:"origin_name,omitempty" toml:"origin_name" yaml:"origin_name,omitempty"`
	Channel       null.String      `boil:"channel" json:"channel,omitempty" toml:"channel" yaml:"channel,omitempty"`

	R *auditPackageGroupR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L auditPackageGroupL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var AuditPackageGroupColumns = struct {
	ID            string
	PackageIds    string
	Operation     string
	Trigger       string
	RequesterID   string
	RequesterName string
	GroupID       string
	CreatedAt     string
	OriginName    string
	Channel       string
}{
	ID:            "id",
	PackageIds:    "package_ids",
	Operation:     "operation",
	Trigger:       "trigger",
	RequesterID:   "requester_id",
	RequesterName: "requester_name",
	GroupID:       "group_id",
	CreatedAt:     "created_at",
	OriginName:    "origin_name",
	Channel:       "channel",
}

// Generated where

type whereHelpertypes_Int64Array struct{ field string }

func (w whereHelpertypes_Int64Array) EQ(x types.Int64Array) qm.QueryMod {
	return qmhelper.WhereNullEQ(w.field, false, x)
}
func (w whereHelpertypes_Int64Array) NEQ(x types.Int64Array) qm.QueryMod {
	return qmhelper.WhereNullEQ(w.field, true, x)
}
func (w whereHelpertypes_Int64Array) IsNull() qm.QueryMod    { return qmhelper.WhereIsNull(w.field) }
func (w whereHelpertypes_Int64Array) IsNotNull() qm.QueryMod { return qmhelper.WhereIsNotNull(w.field) }
func (w whereHelpertypes_Int64Array) LT(x types.Int64Array) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LT, x)
}
func (w whereHelpertypes_Int64Array) LTE(x types.Int64Array) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LTE, x)
}
func (w whereHelpertypes_Int64Array) GT(x types.Int64Array) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GT, x)
}
func (w whereHelpertypes_Int64Array) GTE(x types.Int64Array) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GTE, x)
}

var AuditPackageGroupWhere = struct {
	ID            whereHelperint
	PackageIds    whereHelpertypes_Int64Array
	Operation     whereHelpernull_String
	Trigger       whereHelpernull_String
	RequesterID   whereHelpernull_Int64
	RequesterName whereHelpernull_String
	GroupID       whereHelpernull_Int64
	CreatedAt     whereHelpernull_Time
	OriginName    whereHelpernull_String
	Channel       whereHelpernull_String
}{
	ID:            whereHelperint{field: `id`},
	PackageIds:    whereHelpertypes_Int64Array{field: `package_ids`},
	Operation:     whereHelpernull_String{field: `operation`},
	Trigger:       whereHelpernull_String{field: `trigger`},
	RequesterID:   whereHelpernull_Int64{field: `requester_id`},
	RequesterName: whereHelpernull_String{field: `requester_name`},
	GroupID:       whereHelpernull_Int64{field: `group_id`},
	CreatedAt:     whereHelpernull_Time{field: `created_at`},
	OriginName:    whereHelpernull_String{field: `origin_name`},
	Channel:       whereHelpernull_String{field: `channel`},
}

// AuditPackageGroupRels is where relationship names are stored.
var AuditPackageGroupRels = struct {
	Origin string
}{
	Origin: "Origin",
}

// auditPackageGroupR is where relationships are stored.
type auditPackageGroupR struct {
	Origin *Origin
}

// NewStruct creates a new relationship struct
func (*auditPackageGroupR) NewStruct() *auditPackageGroupR {
	return &auditPackageGroupR{}
}

// auditPackageGroupL is where Load methods for each relationship are stored.
type auditPackageGroupL struct{}

var (
	auditPackageGroupColumns               = []string{"id", "package_ids", "operation", "trigger", "requester_id", "requester_name", "group_id", "created_at", "origin_name", "channel"}
	auditPackageGroupColumnsWithoutDefault = []string{"package_ids", "operation", "trigger", "requester_id", "requester_name", "group_id", "origin_name", "channel"}
	auditPackageGroupColumnsWithDefault    = []string{"id", "created_at"}
	auditPackageGroupPrimaryKeyColumns     = []string{"id"}
)

type (
	// AuditPackageGroupSlice is an alias for a slice of pointers to AuditPackageGroup.
	// This should generally be used opposed to []AuditPackageGroup.
	AuditPackageGroupSlice []*AuditPackageGroup
	// AuditPackageGroupHook is the signature for custom AuditPackageGroup hook methods
	AuditPackageGroupHook func(context.Context, boil.ContextExecutor, *AuditPackageGroup) error

	auditPackageGroupQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	auditPackageGroupType                 = reflect.TypeOf(&AuditPackageGroup{})
	auditPackageGroupMapping              = queries.MakeStructMapping(auditPackageGroupType)
	auditPackageGroupPrimaryKeyMapping, _ = queries.BindMapping(auditPackageGroupType, auditPackageGroupMapping, auditPackageGroupPrimaryKeyColumns)
	auditPackageGroupInsertCacheMut       sync.RWMutex
	auditPackageGroupInsertCache          = make(map[string]insertCache)
	auditPackageGroupUpdateCacheMut       sync.RWMutex
	auditPackageGroupUpdateCache          = make(map[string]updateCache)
	auditPackageGroupUpsertCacheMut       sync.RWMutex
	auditPackageGroupUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var auditPackageGroupBeforeInsertHooks []AuditPackageGroupHook
var auditPackageGroupBeforeUpdateHooks []AuditPackageGroupHook
var auditPackageGroupBeforeDeleteHooks []AuditPackageGroupHook
var auditPackageGroupBeforeUpsertHooks []AuditPackageGroupHook

var auditPackageGroupAfterInsertHooks []AuditPackageGroupHook
var auditPackageGroupAfterSelectHooks []AuditPackageGroupHook
var auditPackageGroupAfterUpdateHooks []AuditPackageGroupHook
var auditPackageGroupAfterDeleteHooks []AuditPackageGroupHook
var auditPackageGroupAfterUpsertHooks []AuditPackageGroupHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *AuditPackageGroup) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range auditPackageGroupBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *AuditPackageGroup) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range auditPackageGroupBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *AuditPackageGroup) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range auditPackageGroupBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *AuditPackageGroup) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range auditPackageGroupBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *AuditPackageGroup) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range auditPackageGroupAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *AuditPackageGroup) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range auditPackageGroupAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *AuditPackageGroup) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range auditPackageGroupAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *AuditPackageGroup) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range auditPackageGroupAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *AuditPackageGroup) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range auditPackageGroupAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddAuditPackageGroupHook registers your hook function for all future operations.
func AddAuditPackageGroupHook(hookPoint boil.HookPoint, auditPackageGroupHook AuditPackageGroupHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		auditPackageGroupBeforeInsertHooks = append(auditPackageGroupBeforeInsertHooks, auditPackageGroupHook)
	case boil.BeforeUpdateHook:
		auditPackageGroupBeforeUpdateHooks = append(auditPackageGroupBeforeUpdateHooks, auditPackageGroupHook)
	case boil.BeforeDeleteHook:
		auditPackageGroupBeforeDeleteHooks = append(auditPackageGroupBeforeDeleteHooks, auditPackageGroupHook)
	case boil.BeforeUpsertHook:
		auditPackageGroupBeforeUpsertHooks = append(auditPackageGroupBeforeUpsertHooks, auditPackageGroupHook)
	case boil.AfterInsertHook:
		auditPackageGroupAfterInsertHooks = append(auditPackageGroupAfterInsertHooks, auditPackageGroupHook)
	case boil.AfterSelectHook:
		auditPackageGroupAfterSelectHooks = append(auditPackageGroupAfterSelectHooks, auditPackageGroupHook)
	case boil.AfterUpdateHook:
		auditPackageGroupAfterUpdateHooks = append(auditPackageGroupAfterUpdateHooks, auditPackageGroupHook)
	case boil.AfterDeleteHook:
		auditPackageGroupAfterDeleteHooks = append(auditPackageGroupAfterDeleteHooks, auditPackageGroupHook)
	case boil.AfterUpsertHook:
		auditPackageGroupAfterUpsertHooks = append(auditPackageGroupAfterUpsertHooks, auditPackageGroupHook)
	}
}

// One returns a single auditPackageGroup record from the query.
func (q auditPackageGroupQuery) One(ctx context.Context, exec boil.ContextExecutor) (*AuditPackageGroup, error) {
	o := &AuditPackageGroup{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for audit_package_group")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all AuditPackageGroup records from the query.
func (q auditPackageGroupQuery) All(ctx context.Context, exec boil.ContextExecutor) (AuditPackageGroupSlice, error) {
	var o []*AuditPackageGroup

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to AuditPackageGroup slice")
	}

	if len(auditPackageGroupAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all AuditPackageGroup records in the query.
func (q auditPackageGroupQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count audit_package_group rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q auditPackageGroupQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if audit_package_group exists")
	}

	return count > 0, nil
}

// Origin pointed to by the foreign key.
func (o *AuditPackageGroup) Origin(mods ...qm.QueryMod) originQuery {
	queryMods := []qm.QueryMod{
		qm.Where("name=?", o.OriginName),
	}

	queryMods = append(queryMods, mods...)

	query := Origins(queryMods...)
	queries.SetFrom(query.Query, "\"origins\"")

	return query
}

// LoadOrigin allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (auditPackageGroupL) LoadOrigin(ctx context.Context, e boil.ContextExecutor, singular bool, maybeAuditPackageGroup interface{}, mods queries.Applicator) error {
	var slice []*AuditPackageGroup
	var object *AuditPackageGroup

	if singular {
		object = maybeAuditPackageGroup.(*AuditPackageGroup)
	} else {
		slice = *maybeAuditPackageGroup.(*[]*AuditPackageGroup)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &auditPackageGroupR{}
		}
		if !queries.IsNil(object.OriginName) {
			args = append(args, object.OriginName)
		}

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &auditPackageGroupR{}
			}

			for _, a := range args {
				if queries.Equal(a, obj.OriginName) {
					continue Outer
				}
			}

			if !queries.IsNil(obj.OriginName) {
				args = append(args, obj.OriginName)
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(qm.From(`origins`), qm.WhereIn(`name in ?`, args...))
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Origin")
	}

	var resultSlice []*Origin
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Origin")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for origins")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for origins")
	}

	if len(auditPackageGroupAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Origin = foreign
		if foreign.R == nil {
			foreign.R = &originR{}
		}
		foreign.R.OriginAPG = append(foreign.R.OriginAPG, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.OriginName, foreign.Name) {
				local.R.Origin = foreign
				if foreign.R == nil {
					foreign.R = &originR{}
				}
				foreign.R.OriginAPG = append(foreign.R.OriginAPG, local)
				break
			}
		}
	}

	return nil
}

// SetOrigin of the auditPackageGroup to the related item.
// Sets o.R.Origin to related.
// Adds o to related.R.OriginAPG.
func (o *AuditPackageGroup) SetOrigin(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Origin) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"audit_package_group\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"origin_name"}),
		strmangle.WhereClause("\"", "\"", 2, auditPackageGroupPrimaryKeyColumns),
	)
	values := []interface{}{related.Name, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.OriginName, related.Name)
	if o.R == nil {
		o.R = &auditPackageGroupR{
			Origin: related,
		}
	} else {
		o.R.Origin = related
	}

	if related.R == nil {
		related.R = &originR{
			OriginAPG: AuditPackageGroupSlice{o},
		}
	} else {
		related.R.OriginAPG = append(related.R.OriginAPG, o)
	}

	return nil
}

// RemoveOrigin relationship.
// Sets o.R.Origin to nil.
// Removes o from all passed in related items' relationships struct (Optional).
func (o *AuditPackageGroup) RemoveOrigin(ctx context.Context, exec boil.ContextExecutor, related *Origin) error {
	var err error

	queries.SetScanner(&o.OriginName, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("origin_name")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.R.Origin = nil
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.OriginAPG {
		if queries.Equal(o.OriginName, ri.OriginName) {
			continue
		}

		ln := len(related.R.OriginAPG)
		if ln > 1 && i < ln-1 {
			related.R.OriginAPG[i] = related.R.OriginAPG[ln-1]
		}
		related.R.OriginAPG = related.R.OriginAPG[:ln-1]
		break
	}
	return nil
}

// AuditPackageGroups retrieves all the records using an executor.
func AuditPackageGroups(mods ...qm.QueryMod) auditPackageGroupQuery {
	mods = append(mods, qm.From("\"audit_package_group\""))
	return auditPackageGroupQuery{NewQuery(mods...)}
}

// FindAuditPackageGroup retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindAuditPackageGroup(ctx context.Context, exec boil.ContextExecutor, iD int, selectCols ...string) (*AuditPackageGroup, error) {
	auditPackageGroupObj := &AuditPackageGroup{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"audit_package_group\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, auditPackageGroupObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from audit_package_group")
	}

	return auditPackageGroupObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *AuditPackageGroup) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no audit_package_group provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if queries.MustTime(o.CreatedAt).IsZero() {
			queries.SetScanner(&o.CreatedAt, currTime)
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(auditPackageGroupColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	auditPackageGroupInsertCacheMut.RLock()
	cache, cached := auditPackageGroupInsertCache[key]
	auditPackageGroupInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			auditPackageGroupColumns,
			auditPackageGroupColumnsWithDefault,
			auditPackageGroupColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(auditPackageGroupType, auditPackageGroupMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(auditPackageGroupType, auditPackageGroupMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"audit_package_group\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"audit_package_group\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into audit_package_group")
	}

	if !cached {
		auditPackageGroupInsertCacheMut.Lock()
		auditPackageGroupInsertCache[key] = cache
		auditPackageGroupInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the AuditPackageGroup.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *AuditPackageGroup) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	auditPackageGroupUpdateCacheMut.RLock()
	cache, cached := auditPackageGroupUpdateCache[key]
	auditPackageGroupUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			auditPackageGroupColumns,
			auditPackageGroupPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update audit_package_group, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"audit_package_group\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, auditPackageGroupPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(auditPackageGroupType, auditPackageGroupMapping, append(wl, auditPackageGroupPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update audit_package_group row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for audit_package_group")
	}

	if !cached {
		auditPackageGroupUpdateCacheMut.Lock()
		auditPackageGroupUpdateCache[key] = cache
		auditPackageGroupUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q auditPackageGroupQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for audit_package_group")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for audit_package_group")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o AuditPackageGroupSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), auditPackageGroupPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"audit_package_group\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, auditPackageGroupPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in auditPackageGroup slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all auditPackageGroup")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *AuditPackageGroup) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no audit_package_group provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if queries.MustTime(o.CreatedAt).IsZero() {
			queries.SetScanner(&o.CreatedAt, currTime)
		}
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(auditPackageGroupColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	auditPackageGroupUpsertCacheMut.RLock()
	cache, cached := auditPackageGroupUpsertCache[key]
	auditPackageGroupUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			auditPackageGroupColumns,
			auditPackageGroupColumnsWithDefault,
			auditPackageGroupColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			auditPackageGroupColumns,
			auditPackageGroupPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert audit_package_group, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(auditPackageGroupPrimaryKeyColumns))
			copy(conflict, auditPackageGroupPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"audit_package_group\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(auditPackageGroupType, auditPackageGroupMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(auditPackageGroupType, auditPackageGroupMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert audit_package_group")
	}

	if !cached {
		auditPackageGroupUpsertCacheMut.Lock()
		auditPackageGroupUpsertCache[key] = cache
		auditPackageGroupUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single AuditPackageGroup record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *AuditPackageGroup) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no AuditPackageGroup provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), auditPackageGroupPrimaryKeyMapping)
	sql := "DELETE FROM \"audit_package_group\" WHERE \"id\"=$1"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from audit_package_group")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for audit_package_group")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q auditPackageGroupQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no auditPackageGroupQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from audit_package_group")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for audit_package_group")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o AuditPackageGroupSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no AuditPackageGroup slice provided for delete all")
	}

	if len(o) == 0 {
		return 0, nil
	}

	if len(auditPackageGroupBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), auditPackageGroupPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"audit_package_group\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, auditPackageGroupPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from auditPackageGroup slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for audit_package_group")
	}

	if len(auditPackageGroupAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *AuditPackageGroup) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindAuditPackageGroup(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *AuditPackageGroupSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := AuditPackageGroupSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), auditPackageGroupPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"audit_package_group\".* FROM \"audit_package_group\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, auditPackageGroupPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in AuditPackageGroupSlice")
	}

	*o = slice

	return nil
}

// AuditPackageGroupExists checks if the AuditPackageGroup row exists.
func AuditPackageGroupExists(ctx context.Context, exec boil.ContextExecutor, iD int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"audit_package_group\" where \"id\"=$1 limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, iD)
	}

	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if audit_package_group exists")
	}

	return exists, nil
}
